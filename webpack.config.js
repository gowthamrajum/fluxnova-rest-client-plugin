const path = require('path');

module.exports = {
  entry: './client/index.js',
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: 'client.js'
  },
  module: {
    rules: [
      {
        test: /\.js$/,
        exclude: /node_modules/,
        use: { loader: 'babel-loader' }
      },
      {
        test: /\.css$/,
        use: ['style-loader', 'css-loader']
      }
    ]
  },
  // React comes from the app (via camunda-modeler-plugin-helpers/react shim), so we do NOT
  // bundle our own copy — the helper modules read the app's globals at runtime.
  performance: { hints: false }
};
